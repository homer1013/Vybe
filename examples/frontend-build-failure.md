# Frontend Build Failure

```bash
vybe r npm run build
vybe errors
vybe prompt explain --redact
vybe share --json --errors --redact
```

Use this for webpack/vite/tsc output where you want structured context for tools.
