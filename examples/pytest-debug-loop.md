# Pytest Debug Loop

```bash
vybe r pytest -q
vybe errors
vybe prompt debug --redact
vybe share --redact --errors --clip
vybe rr
```

Use this when tests fail and you want a fast human+LLM handoff loop.
